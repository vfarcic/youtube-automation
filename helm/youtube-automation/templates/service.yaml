apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtube-automation.fullname" . }}
  labels:
    {{- include "youtube-automation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "youtube-automation.selectorLabels" . | nindent 4 }}
